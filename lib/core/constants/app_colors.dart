import 'package:flutter/material.dart';

class AppColors {
  // ── Brand ───────────────────────────────────────────────
  static const Color primary      = Color(0xFF8D5B4C); // Warm Terracotta
  static const Color primaryLight = Color(0xFFA37060); // Soft Clay
  static const Color primaryDark  = Color(0xFF6F4438); // Deep Terracotta
  static const Color accent       = Color(0xFFD4A373); // Warm Sand / Gold

  // ── Light Mode ──────────────────────────────────────────
  static const Color background   = Color(0xFFFDFBF9); // Warm Alabaster
  static const Color surface      = Colors.white;
  static const Color error        = Colors.red;

  static const Color textPrimary   = Color(0xFF2D2A26); // Soft Ink / Charcoal
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint      = Color(0xFFBDBDBD);

  static const Color divider = Color(0xFFE5E2DE);
  static const Color border  = Color(0xFFE5E2DE);

  // ── Dark Mode ───────────────────────────────────────────
  static const Color darkBackground   = Color(0xFF171514); // Deep Espresso
  static const Color darkSurface      = Color(0xFF221F1E); // Warm Dark Ash
  static const Color darkSurfaceCard  = Color(0xFF2E2A28); // Warm Cocoa Card

  static const Color darkTextPrimary   = Color(0xFFEEEEEE);
  static const Color darkTextSecondary = Color(0xFFAAAAAA);
  static const Color darkTextHint      = Color(0xFF666666);

  static const Color darkDivider = Color(0xFF3A3A3A);
  static const Color darkBorder  = Color(0xFF3A3A3A);
}
